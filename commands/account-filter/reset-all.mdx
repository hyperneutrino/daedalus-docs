---
title: "/account-filter reset-all"
---

<Note>By default, this command requires the **Manage Server** permission.</Note>

Reset all users' exemption from the account filter. The filter still only applies to users whose account age is below the threshold.

## Syntax

```
/account-filter reset-all
```
